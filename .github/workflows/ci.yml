name: CI Pipeline

on:
  push:
    branches:
      - main
      - 'feat/**'
      - 'fix/**'
      - 'chore/**'
  pull_request:
    branches: main
  # workflow_dispatch:
jobs:
  terraform:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow desactivado temporalmente"


# jobs:
#   # Job 1: Linting y formateo
#   lint:
#     name: Lint & Format Check
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'

#       - name: Cache pip packages
#         uses: actions/cache@v4
#         with:
#           path: ~/.cache/pip
#           key: ${{ runner.os }}-pip-lint-${{ hashFiles('requirements.txt') }}
#           restore-keys: |
#             ${{ runner.os }}-pip-lint-

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install ruff

#       - name: Run Ruff linter
#         run: ruff check . --output-format=github

#       - name: Run Ruff formatter check
#         run: ruff format --check .

#   # Job 2: Tests unitarios
#   test-unit:
#     name: Unit Tests
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'

#       - name: Cache pip packages
#         uses: actions/cache@v4
#         with:
#           path: ~/.cache/pip
#           key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
#           restore-keys: |
#             ${{ runner.os }}-pip-

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt
#           pip install pytest pytest-cov

#       - name: Run unit tests
#         run: |
#           pytest tests/ -m unit --cov=src --cov-report=xml --cov-report=term-missing

#       - name: Upload coverage
#         uses: codecov/codecov-action@v4
#         with:
#           file: ./coverage.xml
#           flags: unittests
#           fail_ci_if_error: false

#   # Job 3: Tests de integración
#   test-integration:
#     name: Integration Tests
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'

#       - name: Cache pip packages
#         uses: actions/cache@v4
#         with:
#           path: ~/.cache/pip
#           key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
#           restore-keys: |
#             ${{ runner.os }}-pip-

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt
#           pip install pytest pytest-cov

#       - name: Run integration tests
#         run: |
#           pytest tests/ -m integration --cov=src --cov-report=term-missing
